#include "main.h"

/**
 * _memset - This function fills the first n byte of a memory
 * area pointed to by s with the constant byte b.
 *
 * Return: Always (0) success.
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
