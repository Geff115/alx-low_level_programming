#include "main.h"

/**
 * _strspn - This function calculate the length (in bytes)
 * of the initial segment of s.
 *
 * Return: Always (0) success.
 */
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
