#include "main.h"

/**
 * _strchr - This function finds the first occurence of the
 * character c in the string s.
 *
 * Return: A pointer. Success.
 */
char *_strchr(char *s, char c)
{
	return (NULL);
}
