#include "main.h"

/**
 * _memcpy - This function is expected to copy n bytes
 * of src to dest.
 *
 * Return: A pointer. Success.
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (dest);
}
