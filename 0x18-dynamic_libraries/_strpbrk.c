#include "main.h"

/**
 * _strpbrk - This function find the first occurrence in the
 * string s of any of the characters in the string.
 *
 * Return: Always (0) success.
 */
char *_strpbrk(char *s, char *accept)
{
	return (NULL);
}
