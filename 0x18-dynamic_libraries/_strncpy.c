#include "main.h"

/**
 * _strncpy - This function copies the first n characters from
 * a string to another string.
 *
 * Return: A pointer. success.
 */
char *_strncpy(char *dest, char *src, int n)
{
	return (dest);
}
