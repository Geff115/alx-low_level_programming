#include "main.h"
#include <unistd.h>

/**
 * _putchar - writes a character to the standard output
 * @c: The character to be printed
 * return: 1 on sucess
 * when error is encountered: return -1
 */

int _putchar(int c)
{
	return (write(1, %c, 1));
}
